[project]
name = "genai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "autogen>=0.9.7",
    "django>=6.0",
    "psycopg[binary]>=3.3.2",
    "psycopg-pool>=3.3.0",
    "python-dotenv>=1.1.1",
]
